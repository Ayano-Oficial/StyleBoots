function copiarTexto001() {
    var codigos = document.getElementsByClassName("codigo001");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}



function copiarTexto002() {
    var codigos = document.getElementsByClassName("codigo002");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}



function copiarTexto003() {
    var codigos = document.getElementsByClassName("codigo003");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto004() {
    var codigos = document.getElementsByClassName("codigo004");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}


function copiarTexto005() {
    var codigos = document.getElementsByClassName("codigo005");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}


function copiarTexto006() {
    var codigos = document.getElementsByClassName("codigo006");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto007() {
    var codigos = document.getElementsByClassName("codigo007");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto008() {
    var codigos = document.getElementsByClassName("codigo008");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto009() {
    var codigos = document.getElementsByClassName("codigo009");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto010() {
    var codigos = document.getElementsByClassName("codigo010");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}


function copiarTexto011() {
    var codigos = document.getElementsByClassName("codigo011");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto012() {
    var codigos = document.getElementsByClassName("codigo012");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}


function copiarTexto013() {
    var codigos = document.getElementsByClassName("codigo013");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}


function copiarTexto014() {
    var codigos = document.getElementsByClassName("codigo014");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto015() {
    var codigos = document.getElementsByClassName("codigo015");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}



function copiarTexto016() {
    var codigos = document.getElementsByClassName("codigo016");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}

function copiarTexto017() {
    var codigos = document.getElementsByClassName("codigo017");
    if (codigos.length > 0) {
        var codigo = codigos[0];
        var texto = codigo.innerText;

        navigator.clipboard.writeText(texto)
            .then(function() {
                Swal.fire('Copiado Com Sucesso');
            })
            .catch(function(err) {
                alert("Erro ao copiar o texto: " + err);
            });
    }
}